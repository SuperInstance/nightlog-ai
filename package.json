{ "name": "nightlog-ai", "version": "0.1.0", "private": true, "description": "Dream journal and sleep companion — a cocapn vessel", "scripts": { "dev": "wrangler dev", "deploy": "wrangler deploy" }, "devDependencies": { "wrangler": "^3.0.0", "@cloudflare/workers-types": "^4.0.0" } }
